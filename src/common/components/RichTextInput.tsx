import { Controller } from 'react-hook-form'

interface Prop {
  control?: any
  controlField?: string
}

export default function RichTextInput(props: Prop) {
  const { control, controlField } = props

  return <div>abcd</div>
}
